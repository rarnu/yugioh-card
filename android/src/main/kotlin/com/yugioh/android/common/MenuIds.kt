package com.yugioh.android.common

import android.view.Menu

object MenuIds {

    const val MENUID_SEARCH = Menu.FIRST + 1
    const val MENUID_RESET = Menu.FIRST + 2
    const val MENUID_RESET_DUEL = Menu.FIRST + 3
    const val MENUID_UPDATE = Menu.FIRST + 4
    const val MENUID_SHARE = Menu.FIRST + 5
    const val MENUID_REFRESH = Menu.FIRST + 6
    const val MENUID_FAV = Menu.FIRST + 7
    const val MENUID_SEND = Menu.FIRST + 8
}
