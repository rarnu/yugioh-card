package com.yugioh.android.common

import android.view.Menu

object MenuIds {

    val MENUID_SEARCH = Menu.FIRST + 1
    val MENUID_RESET = Menu.FIRST + 2
    val MENUID_RESET_DUEL = Menu.FIRST + 3
    val MENUID_UPDATE = Menu.FIRST + 4
    val MENUID_SHARE = Menu.FIRST + 5
    val MENUID_REFRESH = Menu.FIRST + 6
    val MENUID_FAV = Menu.FIRST + 7
    val MENUID_SEND = Menu.FIRST + 8
}
