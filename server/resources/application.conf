ktor {
    deployment {
        port = 80
        port = ${?PORT}
        sslPort = 443
        sslPort = ${?PORT_SSL}
    }
    application {
        modules = [ com.rarnu.ygo.server.ApplicationKt.module ]
    }
    security {
        ssl {
            keyStore = /etc/letsencrypt/live/helaoshi.libaoheng.com/keystore.jks
            keyAlias = rarnu
            keyStorePassword = "@cw424.OMI"
            privateKeyPassword = "@cw424.OMI"
        }
    }
    database {
        driver = "com.mysql.cj.jdbc.Driver"
        url = "jdbc:mysql://localhost:3306/YGOData?useUnicode=true&characterEncoding=UTF-8"
        user = "root"
        password = "root"
    }
    network {
        curl = "/usr/bin/curl"
        mono = "/usr/local/bin/mono"
        wrapper = "/usr/local/bin/ygowrapper"
    }
}
